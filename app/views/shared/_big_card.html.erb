<div class="big-card border border-primary d-flex">
  <div class="offers p-3">
    <h3 class="games-offered">Games Offered</h3>
    <%# <p>Title: <%= @offer.title</p> %>
    <%# <p>Description: <%= @offer.description</p> %>
    <div class="offer1">
      <p><%= @offer.game.name %></p>
      <p><%= @offer.duration %>h</p>
      <p>¥<%= @offer.price %></p>
    </div>
    <div class="card-form justify-content-center text-white mt-5">
      <%= simple_form_for([@offer, @booking]) do |f| %>
        <%= f.input :date, label: 'Choose Date', html5: true %>
        <%= f.submit 'Book Now', class: "btn btn-primary w-100 btn-player" %>
      <% end %>
    </div>
  </div>
</div>
