<div class="booking-info">
  <div class="booking-info-title text-white">
    <%= booking.offer.game.name %>
  </div>
  <div class="booking-info-details text-white">
    Date: <%= booking.date %>
    <%= link_to 'x', booking_path(booking), data: { turbo_method: :delete, turbo_confirm: 'Delete?' }  %>
  </div>
</div>
